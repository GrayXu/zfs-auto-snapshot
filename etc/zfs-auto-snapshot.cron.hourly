#!/bin/sh

# Only call zfs-auto-snapshot if it's available
which zfs-auto-snapshot > /dev/null || exit 0
source /etc/profile
zfs-auto-snapshot --quiet --syslog --label=hourly --keep=24 ${zfs_auto_snapshot_target:-//}
